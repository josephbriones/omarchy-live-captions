name: Bug report
description: Report a reproducible Live Captions problem without sharing captured speech.
title: "[Bug]: "
labels:
  - bug
body:
  - type: markdown
    attributes:
      value: Do not attach private speech, usernames, full machine paths, or secrets. Report security issues privately.
  - type: input
    id: plugin-version
    attributes:
      label: Plugin version
      placeholder: 0.1.0
    validations:
      required: true
  - type: input
    id: omarchy-version
    attributes:
      label: Omarchy version
    validations:
      required: true
  - type: input
    id: whisper-version
    attributes:
      label: whisper.cpp / whisper-server version
    validations:
      required: true
  - type: input
    id: model
    attributes:
      label: Model name
      description: Give the model filename, not a private absolute path.
    validations:
      required: true
  - type: dropdown
    id: mode
    attributes:
      label: Mode
      options:
        - Demo (no audio)
        - Microphone
        - Desktop audio
        - Configuration or diagnostics
    validations:
      required: true
  - type: textarea
    id: description
    attributes:
      label: What happened?
      description: Include expected behavior and minimal reproduction steps with private text replaced.
    validations:
      required: true
  - type: textarea
    id: diagnostics
    attributes:
      label: Sanitized diagnostics
      description: Paste doctor output only after checking it for sensitive values.
      render: text
  - type: checkboxes
    id: privacy
    attributes:
      label: Privacy check
      options:
        - label: I removed captured speech, personal information, usernames, sensitive paths, and secrets.
          required: true
